---
name: kernel-html-slides
description: Use when 대본·마크다운·노트·아웃라인을 발표용 슬라이드로 만들어 달라고 할 때. "슬라이드 만들어줘", "이 대본으로 발표자료", "HTML 프레젠테이션", "덱 만들어줘" 등에 쓴다. 읽는 보고서 문서가 필요하면 kernel-html-reports 를 쓴다.
---

# kernel-html-slides

입력 문서를 **1280×720 고정 캔버스의 단일 파일 HTML 슬라이드**로 만든다.
디자인은 `assets/template.html` 에 박제돼 있다.

🔴 **`reference/` 의 HTML 은 사람이 눈으로 보는 것이다. 열어서 읽지 마라.**
갤러리 한 개가 73,000 토큰이고 규칙 파일 다섯을 다 읽어도 17,000 이다.
**형태를 고를 근거는 전부 `assets/*.md` 에 글로 있다.** 사용자가 갤러리를 보고
"이익 풀 형태로" 라고 이름을 말해 주면, 그 이름으로 `exhibits.md` 와 생성기를 쓴다.
사용자가 특정 장을 집어서 물을 때만 그 파일의 해당 부분을 연다.

자매 스킬 `kernel-html-reports` 와 같은 토큰을 쓴다(`assets/theme.md`).

## 절대 원칙

1. **디자인을 발명하지 않는다.** 색·간격·컴포넌트는 템플릿 CSS 만 쓴다. 새 클래스·색 리터럴을 만들지 않는다.
2. **캔버스는 1280×720 고정.** 내용이 넘치면 **폰트를 줄이지 말고 슬라이드를 나눈다.**
3. **단일 파일.** 외부 리소스는 Pretendard 폰트 CDN 하나뿐.
4. **이모지 미사용.**
5. **내용을 지어내지 않는다.** 문구는 입력 문서에서 나온다. 요약·재배치는 하되 없는 사실을 만들지 않는다.
6. **제목은 어절 중간에서 끊기지 않는다.** 한글은 CSS 기본값에서 글자 사이 아무 데나 끊긴다
   (`…것입니다` → `것입니 / 다`). 템플릿에 `word-break:keep-all` 이 박혀 있으니 지우지 않는다.
   두 줄이 필요한 제목은 **문장·어절 경계에 `<br>` 를 직접 넣는다.** 끝줄에 서너 자만 남으면 문구를 줄인다.
7. **그림은 칸 비율에 맞춰 그린다.** SVG 는 viewBox 비율이 담기는 칸과 다르면 축소된다.
   넘치지 않으므로 넘침 검사에 안 걸리는데 **그 안의 글자만 작아져** 가시성이 무너진다.
   본문 슬라이드의 전시물 칸은 대략 **1088 × 340**(3.2:1) 이다. 가로 1080 기준 **높이 330~360** 으로 그린다.
   내용이 그 안에 안 들어가면 캔버스를 늘리지 말고 **두 장으로 나눈다**(원칙 2와 같다).
8. **넘침은 눈으로 판정하지 않는다.** `.slide` 는 `overflow:hidden` 이라 **넘친 줄은 그냥 안 보인다** — 한 줄이 잘려 나가도 화면은 멀쩡하다. `slidecheck.ps1` 로 잰다.
9. **한 요소의 색은 한 곳에서만 드러낸다.** 장의 색은 고스트 번호가 진다. 제목에 밑줄을, 카드에 막대를 겹치지 않는다.
10. **문장도 발명하지 않는다.** 제목·부제·배너는 자리마다 담을 것이 정해져 있다 — `assets/tone.md`. 원본 문서에 있던 문장은 사용자 문장이니 다시 쓰지 않는다.

## 절차

### 1. 입력 확보
변환할 문서를 확인한다. 제목·부제, 청중, 원하는 분량이 있으면 반영하고 없으면 문서 구조에서 추론한다.

### 2. 구조 설계 (만들기 전에)
권장 뼈대: **표지**(cover) → **목차**(agenda, 부/장이 3개 이상일 때) → 부마다 [**구분**(cover) → **본문** 여러 장] → **마무리**(cover).

**본문 한 장의 핵심 요점은 2~4개.** 그보다 많으면 나눈다.

| 내용 성격 | 컴포넌트 |
|---|---|
| 두 개념 대비·비교 | `duo` (2단 패널) |
| 3~4개 병렬 항목 | `cards` |
| 순서·단계 | `flow` 또는 `map` (가로 로드맵) |
| 용어 정의 5~6개 | `wide` + `deflist` |
| 목록형 한 덩어리 | `wide` 패널 1개 |
| 한 문장 결론 | `banner` |
| **숫자를 비교·구성·추세로 말할 때** | **`assets/exhibits.md` 로 간다** (표 3종·그래프 13종) |
| **재무제표를 분석해 보여줄 때** | **`assets/finance-views.md` 로 간다** (CCC·브릿지·듀폰·축구장·민감도 16뷰) |
| **막대로 답이 안 되는 질문일 때** | **`assets/advanced-views.md` 로 간다** (토네이도·확률분포·팬·산키·코호트 13뷰) |

세부 마크업은 `assets/components.md`.

**숫자가 들어가는 장은 `assets/exhibits.md` 를 먼저 본다.** 형태를 고르는 순서(메시지 → 비교 유형 → 형태),
표 열두 줄, 그래프 여덟 줄, 도표 부호 규칙이 거기 있다. 그래프 좌표와 백분율은 손으로 쓰지 않고
`scripts/mkchart.py` 로 뽑는다.

### 3. 채우기
- `assets/template.html` 을 산출 파일로 복사(예: `<문서명>_슬라이드.html`).
- **예시 슬라이드를 전부 지우고** 설계한 슬라이드로 `<div class="deck">` 를 채운다.
- `{{DECK_TITLE}}`·`{{부제}}` 등 `{{...}}` 를 전부 치환한다.
- 첫 슬라이드에만 `class="slide cover active"` 의 `active` 를 남긴다.
- **본문 슬라이드 머리에는 `data-n` 을 준다** — `<div class="head" data-n="01">`. 왼쪽 여백의 고스트 번호가 여기서 나온다. 표지·구분 슬라이드에는 주지 않는다.
- 모든 cover 계열에 `class="... cover"` 가 있는지 확인한다(가운데 정렬 + 부 경계 판정이 이걸 읽는다).
- **부 구분 슬라이드의 eyebrow 는 `<b>제N부 · 이름</b>` 으로 시작한다.** 좌하단 부 표시와 부 진행바 칸 수가 이 문자열에서 나온다. `제N부` 로 시작하지 않는 cover(표지·목차·마무리)는 부를 닫는다.
- 색은 `a1 → a2 → a3` 순환. 경고는 `risk`.
- 문구는 `assets/tone.md` 의 자리별 규칙대로 쓴다. 제목은 사실(숫자 → 벌어진 일 → 명사구) 순으로 고른다.
- 등장 순서대로 `data-anim style="--d:N"` 을 0부터 매긴다.

### 4. 실측 (필수)
```
powershell -ExecutionPolicy Bypass -File <스킬>/assets/slidecheck.ps1 "<C:\절대\경로\슬라이드.html>"
```
슬라이드마다 가장 아래 요소의 바닥과 캔버스 한계를 비교해 여유(px)를 찍는다.

슬라이드마다 **네 가지**를 잰다 — ① 내용이 캔버스를 넘쳤는가 ② **폭이 고정된 라벨 칸이 두 줄로 접혔는가**
③ **제목이 어설프게 끊겼는가**(두 줄 제목의 끝줄에 4자 미만만 남았거나 세 줄이 됐는가)
④ **전시물 SVG 가 축소됐는가**(viewBox 비율이 칸과 달라 85% 미만으로 줄었는가)
⑤ **제목이 오른쪽으로 넘쳤는가**(표지 둘째 줄은 줄바꿈이 막혀 있어 길면 캔버스 밖으로 나간다)
⑥ **문장 중간에서 접혔는가**(문장 경계에서 끊어도 줄 수가 안 느는데 딴 데서 접혔는가)
⑦ **viewBox 밖에 그렸는가**(SVG 는 밖에 그린 것을 잘라 버린다. 줄어드는 것이 아니라 사라진다).

- **라벨 줄바꿈** → `.k`(116px)·`.c`·`.svc .k` 처럼 폭이 고정된 칸에 글자가 많은 것이다.
  **칸을 넓히지 말고 글자를 줄이고, 넘치는 말은 옆 설명 칸으로 옮긴다.** 분량 기준은 `assets/components.md` §7.
- **전시물 축소** → viewBox 높이를 330~360 으로 맞춘다. 그래도 내용이 안 들어가면 두 장으로 나눈다.
  **폰트 크기를 키워서 대응하지 않는다** — 축소 배율이 그대로 곱해지므로 근본 해결이 아니다.
- **가로 넘침** → 표지 둘째 줄(`.mark`)은 `white-space:nowrap` 이라 접히지 않는다. 78px 표지 제목은 **한글 열다섯 자 안쪽**으로 쓴다. 세로 넘침 검사로는 안 잡힌다.
- **문장 줄바꿈** → 두 줄로 흐르는 글은 **문장 경계에서 끊는다.** 줄 수가 느는 자리는 고르지 않는다.
  해당 자리에 `<br>` 를 넣는다. 빌더에서 여러 장을 찍는다면 `build_atlas_deck.py` 의 `sbr()` 처럼 한 곳에서 넣는다.
- **그림 잘림** → viewBox 밖 글자는 안 보인다. 라벨을 안쪽으로 옮기거나 여백을 넓힌다.
  **긴 계열 이름은 오른쪽 여백을 이름 길이에 맞춰 넓혀야 한다**(`mkchart.py` 의 `line()` 참고).
- **제목 꼬리** → 두 줄까지는 좋으나 마지막 줄에 몇 자만 남으면 어설프다.
  **문구를 줄이거나 어절 경계에 `<br>` 를 넣어 문장 단위로 끊는다.** 기준은 `-TailMin` 으로 바꾼다(기본 4자).
- **여유가 음수 = 넘침** → 항목을 줄이거나 슬라이드를 나눈다. **폰트를 줄이지 않는다**(원칙 2).
- **본문 슬라이드의 여유 0 은 정상이다.** 내용 컨테이너가 `flex:1` 로 캔버스를 꽉 채우기 때문이다. 표지는 세로 가운데 정렬이라 여유가 크게 나온다.
- 중첩 상자(패널 안 목록 등)가 자기 칸을 넘어선 경우도 음수로 잡힌다.

> **이 검사기는 `[data-anim]` 을 먼저 중화한다.** 비활성 슬라이드의 `data-anim` 요소는 등장 전 상태(`translateY(18px)`)로 누워 있어서, 중화하지 않고 재면 **모든 슬라이드가 일정하게 넘친 것처럼 보인다.** 값이 여러 장에서 똑같이 나오면 내용 탓이 아니라 측정 탓을 먼저 의심한다.

### 5. 문체 점검 (필수)

제목·부제·배너만 뽑아 한 줄씩 늘어놓고 읽는다. 추출 명령과 판정 기준은 `assets/tone.md` 의 **마감 점검**.
화면은 한 장씩 보여주기 때문에 **반복은 나란히 놓아야만 보인다.**

- 인접한 두 장이 같은 결론에 착지하면 → 뒷장을 되받아치는 장으로 뒤집는다
- `—` 뒤에서 앞말을 다시 설명한 곳이 세 곳을 넘으면 → 공식에 빠진 것이다. 한 번에 고친다
- 제목의 절반 이상이 `…입니다` 요약이면 → 명사구로 내린다

### 6. 검증 (필수)

- [ ] `slidecheck` 판정이 **통과**인가
- [ ] `slidecheck` 의 **라벨 줄바꿈**이 0건인가 (고정폭 칸이 두 줄로 접히면 아래가 잘린다)
- [ ] `slidecheck` 의 **제목 꼬리**가 0건인가 (끝줄에 몇 자만 남은 제목이 없는가)
- [ ] `slidecheck` 의 **전시물 축소**가 0건인가 (그림이 줄어 글자만 작아진 장이 없는가)
- [ ] `slidecheck` 의 **가로 넘침**이 0건인가 (표지 제목이 오른쪽으로 나간 장이 없는가)
- [ ] `slidecheck` 의 **문장 줄바꿈**이 0건인가 (문장 중간에서 접힌 글이 없는가)
- [ ] `slidecheck` 의 **그림 잘림**이 0건인가 (viewBox 밖으로 나가 안 보이는 글자가 없는가)
- [ ] 두 줄 제목이 **문장·어절 경계**에서 끊겼는가. `<br>` 없이 브라우저에 맡기지 않았는가
- [ ] `tone.md` 진단표의 형태(대구·격언·선언형 제목·과장·훈계)가 남아 있지 않은가
- [ ] 숫자를 그린 장이 `exhibits.md` §6 마감 점검을 통과하는가 (막대 축 0, 강조 하나, 단위는 머리행에 한 번)
- [ ] 출처 없는 비율·기간·인원을 지어내지 않았는가
- [ ] 브라우저에서 `←/→`·`↑/↓`·`Home/End`·`F`(전체화면)가 동작하는가
- [ ] `Ctrl+Shift+E` 로 손질 모드가 켜지고, `Ctrl+S` 로 받은 파일이 그대로 열리는가
- [ ] **좌하단 바의 각 칸을 눌러 그 부로 뛰는가.** 표지·목차에서도 눌리는가(거기서 감추면 안 된다)
- [ ] 좌하단에 **현재 부·부 진행바·전체 페이지**가 뜨고, 부 표시는 표지·목차·마무리에서 접히는가
- [ ] 본문 슬라이드마다 `data-n` 이 있고, 표지·구분에는 없는가
- [ ] 표지의 라벨 배지가 **가로로 늘어나지 않았는가**(`.slide` 는 flex column 이라 `align-self:flex-start` 가 필요하다 — 템플릿에 있으니 지우지 않는다)
- [ ] `opacity`·그라데이션 텍스트·색 리터럴을 새로 만들지 않았는가
- [ ] 미치환 `{{...}}`·오타·깨진 태그가 없는가

### 7. PDF
필요하면 헤드리스로 뽑는다(브라우저 인쇄 대화상자의 머리글·바닥글 문제가 없다). 인쇄 CSS 는 이미 들어 있어 슬라이드 하나가 한 페이지가 된다.

```
"<크롬경로>" --headless --disable-gpu --no-pdf-header-footer --virtual-time-budget=12000 --print-to-pdf="<절대경로.pdf>" "<절대경로.html>"
```

## 다 만든 뒤 고쳐 쓰기

문구 하나 고치자고 빌더를 다시 여는 것은 과하다. **고치는 자리는 셋뿐이고, 셋 다 산출 HTML 안에 있다.**

### 1. 문구 — 브라우저에서 바로 (제일 빠르다)

| | |
|---|---|
| `Ctrl` `Shift` `E` | 손질 모드 켜고 끄기 |
| 글자를 누른다 | 그 자리에서 고친다. **차트 안 라벨(SVG)도 된다** |
| `Alt` + 클릭 | **색을 돌린다.** 네이비 → 틸 → 오커 → 클레이 → 회색 → 흐리게 → 원래대로. **도형과 글자 둘 다 된다** |
| 글자를 고르고 `Alt` + 클릭 | **고른 부분만** 칠한다. 고른 상태가 유지되므로 한 번 더 누르면 같은 자리에서 다음 색으로 간다 |
| | 글자는 **오커 → 클레이 → 틸 → 네이비 → 회색 → 흐리게 → 원래대로.** 본문 잉크가 이미 짙은 남색이라 네이비부터 돌면 바뀐 티가 안 난다 |
| `Ctrl` `S` | 고친 상태 그대로 HTML 한 파일로 내려받는다 |
| `PageUp` `PageDown` | 편집 중에도 장을 넘긴다 |

내려받은 파일이 곧 산출물이다. 받은 자리(보통 다운로드 폴더)에서 원래 자리로 옮긴다.
표시 요소는 저장본에 안 남고, 인쇄·`slidecheck` 에도 영향이 없다.

**색은 리터럴로 안 들어간다.** `Alt` + 클릭은 `var(--c-navy)` 같은 토큰 이름을 넣는다.
그래서 나중에 `:root` 를 갈면 손질한 색도 같이 간다. 손으로 색을 넣을 때도 리터럴을 쓰지 않는다.

### 2. 표·색 양식 — `<style>` 블록 한 곳

🔴 **양식만 고칠 때 산출 HTML 을 통째로 읽지 마라.** 슬라이드 본문은 볼 일이 없다.
CSS 는 파일 머리에 다 있으므로 **`<style>` 이 시작하는 곳부터 `</style>` 까지만** 읽고 고친다.
갤러리처럼 큰 파일은 본문이 20만 자를 넘지만 스타일 블록은 그 10분의 1이다.
여러 장에 같은 손질이 필요하면 **빌더의 스킨을 고치고 다시 뽑는다.** 장마다 고치지 않는다.

산출 HTML 은 단일 파일이라 CSS 가 머리에 통째로 들어 있다. 찾을 것은 두 군데뿐이다.

- **색 전체** → `:root` 블록. `--a1/--a2/--a3` 를 바꾸면 표·차트·배지가 한꺼번에 따라온다
- **표 모양** → `.tbl`(본문 표), `.sutbl`(조달·사용 같은 대조표). 여백은 `td` 의 `padding`,
  선은 `border-bottom`, 합계 줄은 `tr.total`

### 3. 그림의 좌표·숫자 — 여기만 빌더로 돌아간다

그림에서 고칠 것은 셋인데 둘은 브라우저에서 끝난다.

| 무엇 | 어디서 |
|---|---|
| 라벨 글자 | 손질 모드에서 클릭 |
| 색·강조 | 손질 모드에서 `Alt` + 클릭. 도형도 글자도 된다 |
| 값·축 범위·백분율 | **생성기.** 계산 결과라 손으로 고치면 라벨과 어긋난다 |


막대 길이나 축 범위처럼 **계산이 필요한 것**은 HTML 에서 고치면 다른 값과 어긋난다.
`scripts/` 의 생성기를 고치고 다시 돌린다.

> **순서를 지킨다.** 빌더를 다시 돌리면 손질한 HTML 이 덮인다.
> **숫자·구조를 먼저 확정하고, 문구 손질은 마지막에** 한다.
> 손질본을 살려야 하면 다른 이름으로 저장해 둔다.

## 내비게이션

**화면 위 조작부가 없다.** 상태를 읽는 자리와 누르는 자리가 좌하단 하나로 합쳐졌다.
(원본 html-slides 의 하단 가로 바도, 이전 판의 오른쪽 세로 레일·점·버튼도 없앴다.)

| | |
|---|---|
| 부 이름 | 좌하단 맨 위 — 현재 `제N부 · 이름`. 부에 속하지 않는 장(표지·목차·마무리)에서는 이름만 감춘다(`visibility`, 자리는 유지) |
| 부 진행바 | **부 수만큼 칸으로 쪼갠다.** 지난 부는 꽉, 현재 부는 진행분만, 남은 부는 빈 칸 — 한 요소가 `전체 몇 부 중 어디`와 `그 부 안에서 얼마나`를 동시에 진다 |
| **바 클릭** | **칸을 누르면 그 부로 뛴다.** 칸 안에서 누른 가로 위치가 그 부의 몇 번째 장인지를 정하므로 **모든 장에 닿는다.** 칸은 16px 높이의 누를 자리, 보이는 3px 선은 그 안의 `span` 이다 |
| 전체 페이지 | 좌하단 맨 아래 `현재 / 전체`. 오른쪽 세로 진행바는 없앴다 — 같은 것을 두 번 그리지 않는다 |
| 키보드 | `←/→`·`↑/↓`·`Space`·`PageUp/Down` 이동, `Home/End` 처음·끝, `F` 전체화면 |
| 터치 | **가로** 스와이프 |
| 딥링크 | `#12` 로 12장부터 연다. 이동할 때마다 주소가 따라 바뀐다 |

> **바는 모든 장에서 보인다.** 표지·목차에서 감추면 거기서 부로 뛸 수가 없다.
> 감출 때 `display:none` 을 쓰면 폭이 0 이 되어 클릭 계산이 `NaN` 이 되고,
> `go(NaN)` 이 **활성 슬라이드를 통째로 없앤다.** `go()` 의 숫자 가드를 지우지 말 것.

인쇄에서는 `.hint`·`.brandbar` 가 숨는다.

## 참고 파일

- `assets/template.html` — CSS·JS·좌하단 표시가 든 뼈대 + 예시 슬라이드
- `assets/components.md` — 컴포넌트 스니펫, **쓰지 말 것 목록**, 이 스킨의 변경점
- `assets/theme.md` — 색 토큰 정본. 스타일 교체는 여기만 본다
- `assets/tone.md` — **문체 정본.** 자리별 규칙·진단표·마감 점검. 자매 스킬도 이걸 쓴다
- `assets/exhibits.md` — **그림 정본.** 비교 유형 다섯, 표 열두 줄, 그래프 여덟 줄, 도표 부호 규칙
- `assets/finance-views.md` — **재무분석 뷰 정본.** 재무제표에서 무엇을 그릴지. 뷰마다 입력·함정
- `assets/advanced-views.md` — **고급 전시물 정본.** 불확실성·가치의 원천·분포·구조. M&A 와 컨설팅에서 쓰는 13종
- `assets/slidecheck.ps1` — 1280×720 넘침 실측기
- `scripts/mkchart.py` — 숫자에서 차트 조각을 뽑는다. 좌표·백분율을 손으로 쓰지 않기 위한 것
- `scripts/build_gallery.py` · `scripts/gallery_specs.json` — 갤러리 재생성
**아래 `reference/` 파일은 전부 사람용이다. 통째로 읽지 않는다.**

- `reference/기준_슬라이드.html` / `.pdf` — 배치 견본. 표지·목차·두 칸·카드를 어떻게 놓는지 눈으로 본다. **규칙 자체는 `assets/components.md` 에 있고 그쪽이 정본이다**
- `reference/전시물_갤러리.html` — 표·그래프·도표 21장. 사용자가 형태를 고를 때 편다. **고르는 근거는 `assets/exhibits.md`(3.9K)** 이고 그것만 읽으면 된다
- `reference/재무그래프_갤러리.html` — 재무 전시물 45종 56장. **230KB 다. 열지 마라.** 사용자가 보고 이름을 말해 주는 용도다. 뷰 이름과 쓰는 때는 `assets/finance-views.md` 와 `assets/advanced-views.md` 에 있다. 빌더는 `scripts/diag/build_atlas_deck.py`
- `reference/전시물_아틀라스_독본.html` — 같은 전시물을 인쇄용 문서로 푼 판. 빌더는 `scripts/diag/build_atlas.py`
- `reference/재무진단_서린푸드.html` — **완성 보고서 기준.** 한 회사를 전시물 9매로 처음부터 끝까지 진단한 편집형 보고서. 빌더는 `scripts/diag/`
- `reference/서린푸드_머니플로우.html` — **머니플로우 기준.** 매출에서 잉여현금까지 생키 한 줄기로 잇고, 새는 자리 3곳을 확대한 뒤, 고친 흐름을 같은 그림으로 다시 그린 판. 흐름이 실제로 분기·합류할 때만 이 형식을 쓴다. 빌더는 `scripts/diag/build_v2.py` + `flow.py`
- `scripts/mkchart_adv.py` — 슬라이드용 고급 전시물 13종 생성기 (1280×720 캔버스 전용)
- `scripts/build_finance.py` · `scripts/build_advanced.py` 와 각 `*_specs.json` — 사양 하나씩 확인할 때 쓰는 부분 덱. 결과는 `reference/parts/` 로 떨어지고 배포하지 않는다
